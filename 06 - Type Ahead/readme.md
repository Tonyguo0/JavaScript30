# Learnt

- `type=module` in script tag to use ES6 module syntax in browser which allows us to use async/await in the top level of our JavaScript code without needing to wrap it in an async function. This can make our code cleaner and easier to read, as we can use async/await directly in our main script without needing to create additional functions or callbacks. this works because when we use `type=module`, the JavaScript code is treated as a module and is executed in strict mode, which allows us to use modern JavaScript features like async/await without any issues. This can help improve the readability and maintainability of our code, as we can write asynchronous code in a more straightforward and intuitive way.
- however we need to make sure that our JavaScript file is served from a web server, as some browsers may not allow us to use ES6 modules when opening the HTML file directly from the file system. This is because of security restrictions that prevent certain features from being used in local files. To avoid this issue, we can use a simple web server like `http-server` or `live-server` to serve our files and enable the use of ES6 modules in the browser. This can help ensure that our code runs smoothly and without any issues related to module loading or security restrictions.
- also variables are scoped to the module when using `type=module`, which means that they are not accessible from the global scope. This can help prevent naming conflicts and improve the overall organization of our code, as we can keep our variables and functions contained within the module without worrying about them interfering with other parts of our code or third-party libraries. This can also help improve the maintainability of our code, as we can easily manage and organize our variables and functions within the module without needing to worry about global scope issues. so cities and findMatches function won't be accessible from other scripts on the page.
- RegExp constructor can be used to create a regular expression object from a string, which allows us to dynamically generate regular expressions based on user input or other variables. This can be particularly useful when we want to create a regular expression that includes special characters or when we want to create a regular expression that is based on user input. For example, in the code snippet provided, we use the RegExp constructor to create a regular expression that matches the user's input in a case-insensitive way. This allows us to search for matches in the cities array without worrying about the case of the input or the city names. By using the RegExp constructor, we can create flexible and dynamic regular expressions that can adapt to different inputs and scenarios, making our code more versatile and powerful.
- use `match()` and `replace()` string methods to find and highlight the matching parts of the city names. The `match()` method is used to search for a match between a regular expression and a string, while the `replace()` method is used to replace a specified value with another value in a string. In the code snippet provided, we use the `match()` method to find the parts of the city name that match the user's input, and then we use the `replace()` method to wrap those matching parts in a `<span>` element with a class of "hl". This allows us to visually highlight the matching parts of the city names in the user interface, making it easier for users to see which cities match their search query. By using these string methods, we can create a more interactive and user-friendly experience for our users when searching for cities.
- `addEventListener()` method is used to attach an event handler to a specified element, in this case, the search input field. This allows us to listen for specific events, such as "input", and execute a function when that event occurs. In the code snippet provided, we use `addEventListener()` to listen for the "input" event on the search input field, which triggers the `displayMatches` function whenever the user types something into the input field. This allows us to dynamically update the list of matching cities as the user types, providing a real-time search experience. By using `addEventListener()`, we can create interactive and responsive web applications that respond to user input and events in a seamless way.
- `innerHTML` property is used to set or get the HTML content of an element. In the code snippet provided, we use `innerHTML` to update the content of the suggestions element with the list of matching cities. When the user types something into the search input field, the `displayMatches` function is called, which generates a list of matching cities and updates the `innerHTML` of the suggestions element to display those matches. This allows us to dynamically update the user interface based on user input, providing a more interactive and engaging experience for our users. By using `innerHTML`, we can easily manipulate the content of our web page and create dynamic and responsive applications.
- `change` and `keyup` change can be found in the HTMLElement documentation, and keyup can be found in the UI events documentation. The `change` event is fired when the value of an input element changes and loses focus, while the `keyup` event is fired when a key is released after being pressed. In the context of the code snippet provided, we use the `input` event to listen for changes in the search input field, which allows us to update the list of matching cities in real-time as the user types. However, if we were to use the `change` event instead, the list of matching cities would only update after the user finishes typing and clicks outside of the input field, which may not provide the same level of interactivity. Similarly, using the `keyup` event would trigger the function every time a key is released, which may not be necessary for our use case and could lead to performance issues if there are many key events being fired. By using the appropriate event type for our specific use case, we can create a more efficient and responsive user experience.